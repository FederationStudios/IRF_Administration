services:
  irf_administration:
    container_name: irf_administration
    build:
      context: ./
      secrets:
        - configFile
    restart: on-failure

secrets:
  configFile:
    file: ${configfile}
